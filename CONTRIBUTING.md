# 贡献指南

感谢你愿意改进 doc2md-ai-organizer。

提交 Pull Request 前，请注意：

1. 不要提交源文档、转换输出、`.env` 文件或任何真实 API Key。
2. 不要加入硬编码的内部 URL、私有模型名、个人路径或业务资料。
3. 尽量把 OCR 能力放在适配器脚本中，不要把主流程绑定到某一个私有服务。
4. 新增运行时依赖时，请同步更新 `THIRD_PARTY_NOTICES.md`。
5. 修改主流程前，请至少用一个小样本目录跑过 `--dry-run`。
