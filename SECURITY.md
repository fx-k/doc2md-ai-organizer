# 安全说明

## 敏感文档

这个工具会读取文档内容，并可能把文档片段发送给 AI 接口，用于生成文件名和目录归类建议。

如果你的文档包含隐私、商业秘密、客户资料、受监管数据或其他敏感信息，请只使用经过你确认和授权的 AI 服务。

## API Key

不要提交 API Key。

推荐使用环境变量或本地 `.env` 文件保存密钥。本仓库的 `.gitignore` 已默认忽略 `.env`。

## 报告安全问题

如果你发现安全问题，请不要在公开 issue 中贴出敏感细节。请通过仓库维护者提供的私密渠道联系。
